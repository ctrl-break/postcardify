/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CategoryAssociationDto } from '../../models/category-association-dto';
import { PageDto } from '../../models/page-dto';
import { PageMetaDto } from '../../models/page-meta-dto';

export interface CategoryControllerFindWordsByCategoryAndFirstLetter$Params {
    id: string;
    letter: string;
    page: string;
    perPage: string;
}

export function categoryControllerFindWordsByCategoryAndFirstLetter(
    http: HttpClient,
    rootUrl: string,
    params: CategoryControllerFindWordsByCategoryAndFirstLetter$Params,
    context?: HttpContext,
): Observable<
    StrictHttpResponse<
        PageDto & {
            data?: Array<CategoryAssociationDto>;
            meta?: PageMetaDto;
        }
    >
> {
    const rb = new RequestBuilder(rootUrl, categoryControllerFindWordsByCategoryAndFirstLetter.PATH, 'get');
    if (params) {
        rb.path('id', params.id, {});
        rb.path('letter', params.letter, {});
        rb.query('page', params.page, {});
        rb.query('perPage', params.perPage, {});
    }

    return http.request(rb.build({ responseType: 'json', accept: 'application/json', context })).pipe(
        filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
        map((r: HttpResponse<any>) => {
            return r as StrictHttpResponse<
                PageDto & {
                    data?: Array<CategoryAssociationDto>;
                    meta?: PageMetaDto;
                }
            >;
        }),
    );
}

categoryControllerFindWordsByCategoryAndFirstLetter.PATH = '/api/v1/words/category/{id}/list/letter/{letter}';
